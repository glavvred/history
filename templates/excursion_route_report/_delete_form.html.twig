<form method="post" action="{{ path('app_excursion_route_report_delete', {'id': excursion_route_report.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ excursion_route_report.id) }}">
    <button class="btn">Delete</button>
</form>
